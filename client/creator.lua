local zones = require 'modules.zone'
local spawnPoint = require 'modules.spawnpoint'
local pedcreator = require 'modules.pedcreator'

lib.callback.register('rhd_garage:', function(source)

    return true
end)